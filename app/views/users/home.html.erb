  <style>
  .bbottom{
    border-bottom:2px solid grey;
  }
  
  </style>
<div class="container">
  <div style="width:100%;height:55px" class="bbottom">
    <div><b>HOME</b>  
    <!-- Right navbar links -->
    <% if @user.role.id == 1 || @user.role.id == 2 %>
    <ul class="navbar-nav ml-auto">
      <!-- Notifications Dropdown Menu -->
        <div>
            <div class="dropdown">
              <a class="btn btn-primary mr-2 " href="#" role="button" id="notificationadmin" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class=" fas fa-bell"></i>
                  Notifications <span class=" p-1" ><sub style="background-color: red; color: white; font-size:medium" id="count1"></sub></span>
              </a>

              <div class="dropdown-menu" id="shownotification1" aria-labelledby="dropdownMenuLink">
                <a class="dropdown-item" href="#">No new notifications</a>
              </div>
            </div>
        </div>
    </ul>
    <% end %>
  </div>
  <br>
<div>
  <div class="jumbotron container">
      <h1 class="display-5">Welcome To Easy Guest</h1>
      <%= link_to "Sign out", session_path(@user.id), method:"delete" ,class:"btn btn-info"%>
  </div>
</div>
</div>